package com.gestor.comprador.data

/**
 * Configuração do aplicativo.
 *
 * URL base do sistema — FIXA (não é exibida na tela de login).
 * O comprador se conecta ao servidor de produção.
 */
object AppConfig {
    const val BASE_URL: String = "https://www.tonyceel.com.br"
}
